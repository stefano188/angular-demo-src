import { StringRangeDirective } from './string-range.directive';

describe('StringRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new StringRangeDirective();
    expect(directive).toBeTruthy();
  });
});
